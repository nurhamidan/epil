<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.my.nurhamidan.epil">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Epil">
        <activity android:name=".views.RegisterActivity"></activity>
        <activity android:name=".views.ExampleActivity" />
        <activity android:name=".views.LoginActivity" /> <!-- <activity android:name=".views.MainActivity2"></activity> -->
        <activity android:name=".views.RegisterUserFormActivity" />
        <activity android:name=".views.CreatePollFormActivity" />
        <activity android:name=".views.HomeActivity" /> <!-- <activity android:name=".views.LoginActivity"></activity> -->
        <activity
            android:name=".Main"
            android:theme="@style/splash_style">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>